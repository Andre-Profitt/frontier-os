# Bin

This directory should eventually hold the operator-facing CLI entrypoint and helper scripts.

Near-term target:

- `frontier`

The CLI should remain a thin command surface over the contracts and adapters defined elsewhere in this repo.
