# Memory

Memory should be structured before it is embedded.

Required memory classes:

- `episodic`
- `procedural`
- `outcome`
- `cross_project`
- `system_health`

Memory is not just recall.

It is the promotion path from repeated traces into reusable operating knowledge.
